# 2026-05-21 -- Session 1 Notes

A long session on the **game setup** feature of Ultimate Monopoly (ASP.NET Core 9
Razor Pages). Covered: a `UserService` extraction refactor; completing
`GameSetupService` (create / join / leave / kick / dice numbers / reorder);
join-by-code; EF indexes across every data model in the assembly; consolidating
result records; and the first game UI — the **Create**, **Setup** and **Join**
Razor pages. Continues from the 2026-05-20 sessions.

Working pattern this session: John builds, the assistant reviews, John fixes —
repeated per increment. Sections below record the outcome, not every cycle.

---

## 1. Session start — context

Read the 2026-05-20 session notes (S1, S2) and the three design docs
(`game-rules.md`, `game-engine.md`, `signalr-design.md`).

`pckg-docs/` (repo root) holds the API/guide markdown for the private `JC.*`
packages — used several times this session (`JC.Core`, `JC.Web`).

---

## 2. UserService — centralising direct DbContext user queries

Searched the services for code hitting `_context.Users` / `db.Users` directly.
John extracted a `Services/UserService.cs` to own those lookups —
`ValidUser(string)`, `CountValidUserIds`, `GetUserDictionary` (single + bulk),
`GetUserById`, `GetUserByUsername` (all with an optional `enabledFilter`).

Refactored to consume it: `ProfileService`, `FriendService`,
`BlockAndReportService`, `BoardSkinShareService`. **`PresenceService` was left
alone deliberately** — it does presence-specific work with `AppUser` and needs
the `DbContext` regardless.

Review findings (all fixed by John):
- `UserService` was not registered in DI — added `TryAddScoped<UserService>()`.
- `ValidateUserIds` had "any-match" semantics that misled given the plural name
  — renamed to `ValidUser(string)` (single-id), callers updated.
- `BoardSkinShareService.ValidUserIds` still queried `_context.Users` directly
  — rewired to `_userService.CountValidUserIds`.

---

## 3. AppDbContext — Games tables

`Game`, `GamePlayer`, `GameTurn`, `GameSnapshot` `DbSet`s added to
`AppDbContext`, the repository contexts registered in `ServiceRegistration`,
and EF migrations created — `20260521103835_GameTables` and the follow-up
`20260521123542_GameJoinCode` (the `JoinCode` column).

---

## 4. GameSetupService — built out across the session

`Services/Games/GameSetupService.cs` grew from a stub to the full setup service.

- **`TryCreateNewGame`** — validates the board skin and the name (via
  `ValidateGame` + `ModelStateWrapper`), creates `Game` + host `GamePlayer` in a
  transaction. *Bug fixed:* QR generation was inside the transaction `try`,
  after the commit — a QR failure triggered a bogus rollback and reported
  failure on an already-created game. Moved the QR step outside the `try`.
- **`TrySetPlayerDiceNumbers`** — validates user + Setup state, sorts the dice
  pair, checks uniqueness, persists. A `lock` was attempted for the
  concurrency race and **removed** — it was sync-over-async, didn't actually
  close the race, and the lock object was per-instance on a scoped service.
- **`TryReorderPlayers`** — public (validates the posted set matches the live
  players) delegating to a private overload that renumbers `OrderId` 0..N-1.
- **`TryJoinGame`** — Setup-state + user validation, idempotent already-joined
  check, capacity check, block check, appends `OrderId = max+1`.
- **`TryLeaveGame` / `TryKickPlayerFromGame`** — consolidated the shared
  soft-delete + reorder logic into a private `TryRemovePlayerFromGame`. The host
  cannot leave; kick is host-only and targets a *different* user. (An earlier
  kick implementation conflated caller and target — fixed.) Player removal now
  **hard-deletes** the `GamePlayer` (no value keeping soft-deleted rows during
  setup).
- **`GetSetupGame`** — read method for the Setup page; includes `Players` +
  `BoardSkin`, and returns the game **only to the host**.

---

## 5. Dice-number uniqueness — Option A (DB unique index)

The custom rules give each player a permanent two-dice "number"; numbers must be
unique within a game. After weighing an app-level gate vs. a DB constraint,
John chose **Option A — a DB unique index**:

- `GamePlayer.Dice1` / `Dice2` made **nullable** (`ushort?`) — unset = `NULL`,
  and MySQL allows multiple `NULL`s in a unique index.
- `SetDiceNumber` **normalises** the pair to sorted order (min, max), so the
  index correctly treats `(3,5)` and `(5,3)` as the same number.
- Unique index `(GameId, Dice1, Dice2)` on `GamePlayer` (see §6).
- `TrySetPlayerDiceNumbers` keeps the in-memory pre-check as a fast path, with
  the unique constraint as the real backstop.

This supersedes the "accepted race" position from earlier in the session — the
race is now closed at the database level.

---

## 6. EF indexes across all data models

Added `[Index]` attributes (no fluent config) to every data model in the
assembly. EF already auto-indexes FK columns, so explicit indexes were added
only for the no-FK `UserId`/`CreatedById`/`ToUserId`/etc. columns, composites,
and the one unique constraint:

- `FriendRequest` — `CreatedById`, `ToUserId`.
- `BlockedUser` — `(CreatedById, BlockedUserId)`, `BlockedUserId`.
- `Friend` — `CreatedById`, `FriendUserId`.
- `BoardSkin` — `UserId`; `SharedBoardSkin` — `UserId`.
- `Game` — `(CreatedById, State)`.
- `GameTurn` — `(GameId, TurnNumber)` — **non-unique on purpose** (backtrack
  soft-deletes turns and reuses turn numbers).
- `GamePlayer` — **`(GameId, Dice1, Dice2)` UNIQUE**, plus `UserId`.

No unique indexes on the social join entities — they are soft-deletable and the
services `new` up fresh rows after a soft-delete, which a table-wide unique
index would collide with (MySQL has no partial index).

---

## 7. ResultRecords.cs — consolidation

Moved all four service result records into `Models/ResultRecords.cs`
(namespace `UltimateMonopoly.Models`): `GameCreationResult`, `JoinGameResult`,
`FriendRequestResult`, `SaveSkinResult` — removed from their respective service
files.

`JoinGameResult` later gained a `GameId` so the join-by-code flow has a game id
to redirect with.

---

## 8. Join-by-code

John added a `JoinCode` to `Game` (prefix `"MP"` + a 5-char code from a
`JoinCodeGenerator`, total 7 chars), set via `SetJoinCode`, with a
collision-retry loop in `TryCreateNewGame`. New service method
`TryJonGameFromCode(joinCode, userId)` looks the game up by code and joins.

*Note:* the method name is misspelled — `TryJonGameFromCode` should be
`TryJoinGameFromCode`.

Discussed code generation: truncating a GUID is weak (hex-only, no uniqueness
guarantee); the chosen approach is a fixed unambiguous alphabet + crypto RNG
(`RandomNumberGenerator.GetItems`) with a DB collision-retry loop.

---

## 9. Setup screen design — signalr-design.md

A **Screen Design** section was added to `signalr-design.md` (the tablet setup
screen). Clarified with John: the host's card gets a primary border and **no
Kick button**; each card has a **Set Dice** button left of **Kick**; the join
code shows below the QR; `Cancel Game` is a placeholder for now.

---

## 10. Game UI — the Razor pages

Decision: **server-render the pages now, layer SignalR on later** (the
`GameHub` does not exist yet). The project has **global authorization**, so no
`[Authorize]` attributes on new pages.

**`Pages/Game/Index.cshtml`** — Create New Game. Two columns: col-8 create form
(name, board picker with "Default board" = null skin, rounding-rule select),
col-4 rounding-rules explainer. Posts to `TryCreateNewGame`, redirects to the
Setup page. Navbar "New Game" link wired to it.

**`Pages/Game/Setup.cshtml`** — the setup screen. Header (name, board, rounding
rule); col player list + col join QR with the join code beneath it; Start /
Cancel footer buttons (placeholders). Player cards: profile circle + display
name, host border + "Host" badge, dice numbers or "Not Set", Set-Dice and
host-only Kick buttons. Interactions wired as ordinary POST handlers
(`OnPostKick` / `OnPostSetDice` / `OnPostReorder`) — with the host/self auth
checks the future hub would do. Added `GameSetupService.GetSetupGame` as the
read method.

**`Pages/Game/Join.cshtml`** — route `/join/{id?}`. With an id: joins that game,
shows a danger card on failure. With no id: renders a join-code entry box; the
input is trimmed + upper-cased and passed to `TryJonGameFromCode`. Both success
paths redirect to `/Game/Waiting` (page not built yet).

---

## 11. Drag-reorder — SortableJS

The native HTML5 drag-and-drop couldn't give the "pick up" feel John wanted.
Added **SortableJS** (`npm install sortablejs`, vendored to
`wwwroot/scripts/Sortable.min.js`, consistent with `signalr.min.js`).
`game-setup.js` now uses `Sortable` with `animation`, `forceFallback` (a real
floating clone for the lift/shadow pickup) and grab/grabbing CSS cursors;
`onEnd` posts the new order only when it changed.

---

## 12. Floating auto-dismiss alerts

The Setup page status alert is now a **floating** toast (fixed top-right) using
the JC.Web `<alert>` tag helper. `SetupModel` exposes `StatusAlertType`
(`StatusKind` string → `AlertType`). `game-setup.js` auto-dismisses it after
5 seconds via `bootstrap.Alert`.

---

## 13. SCSS

`custom-bootstrap.scss` was recompiled several times (`npm run build:css`) after
John adjusted theme colours. Two new colours initially failed Bootstrap's
`color-contrast()` 4.5:1 check (used by the hero buttons); John iterated until
both cleared.

---

## 14. Dice numbers — turn order & input UI

- **Sum uniqueness.** `TrySetPlayerDiceNumbers` now also rejects a roll whose
  two-dice *total* matches another player's — the dice numbers decide **turn
  order** (who starts), so equal totals would tie. The pair / swapped-pair
  checks are subsumed by the sum check. The DB unique index stays on
  `(GameId, Dice1, Dice2)` (pair-level) — John's call: the service validates,
  the index is a structural backstop, not a full mirror of the rule.
- **1–6 button selectors.** Dice entry (Setup modal + Lobby) uses Bootstrap
  `btn-check` radio buttons — Die 1 row above, Die 2 below — instead of number
  inputs. Setup also shows each player's dice as `Dice Numbers: [badge d1 | d2]`.

---

## 15. Player flow — Join and Lobby pages

- **Join page** — danger card showing `JoinGameResult.Message` on failure; with
  no game id it renders a **join-code entry box** (input trimmed + upper-cased →
  `TryJonGameFromCode`). The Setup page shows the join code under the QR.
- **Lobby page** (`Game/Player/Lobby`) — the player's own waiting room: profile
  card, a 1–6 dice-number form, a "waiting for host to start" spinner, Leave.
  Strictly self-only (`OnGet` `Forbid()`s if route user ≠ logged-in user).
- **`PlayerProfileService`** — new; `GetPlayerForGameSetup(gameId, userId)`
  returns the `GamePlayer` (incl. `Game`) for a setup-state game.

---

## 16. ProfileService — cookie-backed profile view model

- The `user-profile` encrypted cookie was **expanded** to carry username +
  display name (was avatar-only), so a `UserProfileViewModel` can be rebuilt
  from it with no DB hit.
- `UserProfileViewModel` gained a field-based constructor; the `AppUser`
  constructor chains to it. New `ProfileService.GetProfileViewModelAsync(userId)`
  — cookie when it's that user, DB otherwise.
- **Bug fixed:** `WriteCookie` took a `userId` param but sourced the name fields
  from `_userInfo` — a mismatch risk. It now drops the param; the cookie is
  always the current user's.

---

## 17. The Game area + thin in-game layout

In-game pages moved into a new **`Game` area** (`Areas/Game/Pages/`) with their
own thin layout: a nav of a primary circular **Go Back** button (left), the
`_Branding` partial (centre), and the profile circle → `/profile` (right). Every
nav click is gated by an "Are you sure you want to leave this page?"
confirmation modal (`game-nav.js`). `Game` (singular) = the area = one game in
progress.

---

## 18. Project restructure

- **Services and hubs** moved out of areas into the root folders — they're
  shared across areas and root.
- **`Pages/Game/` → `Pages/Games/`** (root, plural) for the *non*-in-game pages.
  `Games` = list / management; `Game` = the in-game area.
- **Create New Game (`Index`)** moved out of the area into `Pages/Games/`, URL
  `/new-game` — not an in-game page.
- **Join** moved out of the area into `Pages/Games/`.
- Nav links and `RedirectToPage` targets were fixed for each move — relative
  `./` redirects break when a page leaves the area; the Lobby's home redirect
  needed `area = ""` so it didn't resolve to the Game-area Index.

---

## 19. SignalR hubs — design decision

Agreed to **split the hub per game phase** rather than one lifecycle hub: a
`GameSetupHub` and a `GamePlayHub`, with shared plumbing (group join/leave, host
check) in a base hub; completed games have no hub. The phase boundary matches
the page-navigation boundary. The `StartGame` handoff (setup hub broadcasts →
clients navigate to the in-play page → open the play hub) is the piece to design
carefully. **Not built; `signalr-design.md` not yet updated to this.**

---

## 20. Game lists — service, pages, partials

- **`GameListService`** — `QueryGames` plus `GetAllMyGames` (games you host) and
  `GetAllGamesJoined` (games you're a player in), each with per-state slice
  conveniences. (A copy-paste bug had every `*GamesJoined` method calling
  `GetMyGames` — fixed.)
- **`GameViewModel`** — list view model.
- **`Pages/Games/MyGames`** and **`Pages/Games/Joined`** — tabbed pages
  (Setup | In Play | Finished | Cancelled); one `GetAll*` load, partitioned by
  state in memory.
- Two reusable partials: **`_GameTabs`** (tab strip + panes) and **`_GameList`**
  (renders a `List<GameViewModel>`).
- Navbar Games dropdown reworked: New Game, Join Game, — divider —, My Games,
  Joined Games.
- **In progress (paused mid-edit):** per-card action buttons in `_GameList` —
  Open Host / Player Lobby (Setup & In Play), View (Finished), Delete
  (Cancelled). Delete is My-Games-only via a `ViewData["ShowHostActions"]` flag
  and posts to a stub `OnPostDelete` (no soft-delete backend yet).

---

## 21. Floating alert generalised

The 5-second floating auto-dismiss alert moved from `game-setup.js` into the
shared `game-nav.js` (the Game-area layout's script), keyed off a generic
`data-floating-alert` — every game-area page gets it. The Lobby's status alert
was switched to the same floating style.

---

## Outstanding / next steps

- **No browser testing** — builds compile, but nothing was exercised in a browser.
- **Game list buttons** — `_GameList`'s per-card actions were mid-implementation
  when the session paused; needs finishing.
- **SignalR hubs** — `GameSetupHub` / `GamePlayHub` agreed but not built;
  `signalr-design.md` to be updated to the two-hub design.
- **In-game pages don't exist** — `Game/Player/Index` (in-game player profile,
  `/player-profile/...`) is a stub; there is no in-play host view. So the
  "Open Host" / "Player Lobby" buttons on *In Play* games, and a Join landing on
  the Lobby after a game starts, point at setup-only pages.
- **Cancelled-game delete** — `OnPostDelete` is a stub; no soft-delete backend.
- **Start / Cancel on Setup** — placeholder buttons; need `TryStartGame` /
  `TryCancelGame` + the in-game view.
- **Rename `TryJonGameFromCode`** → `TryJoinGameFromCode`.
- Minor: `TryCreateNewGame`'s join-code collision query omits `FilterDeleted`.
