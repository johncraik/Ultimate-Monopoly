# 2026-06-10 -- Session 1 Notes

A web/UI session on top of last session's stats engine. Three strands: the
**game-lifecycle host controls** (Force Refresh / Cancel / Delete) wired
end-to-end, the **recurring stats projection job**, and the session's
centrepiece — the **all-players stats comparison page**, built on a new
**shared stat catalogue** that the comparison table drives off (and the
single-player view will too). Bookended by a **TODO/unfinished-work review** and
two bits of play-page polish (scrollable player list, board rent-level
indicator).

Working pattern: fast, collaborative iteration. John rewrote `GameService`
mid-flight (twice my edits to it were clobbered and re-applied — see §2). Two
sharp corrections steered the work: **use the existing `GameSetupService.TryCancelGame`**
rather than re-injecting `GameService` ("READ THE FUCKING CODE"), and **mask via
the empty `UserId` already on the placeholder VM** rather than adding a new flag.

---

## 1. 🌟 Force Refresh — host panel → hub → off-pump broadcast

Wired the host panel's Force Refresh button:
- **`IEngineNotifier.ForceRefresh(gameId)`** added (the broadcast-to-clients seam,
  beside `GameCompleted`) + `SignalrEngineNotifier` impl (`ForceRefreshMessage`).
  Interface change ⇒ the **three `NoOpNotifier` test fakes** each got a one-line
  no-op (recurring tax of touching `IEngineNotifier`).
- **`GameService.ForceRefresh(gameId, submittingUserId)`** — host-gated, broadcasts
  **directly (not enqueued)**. The key call: enqueuing would sit behind a parked
  prompt or a wedged work item — exactly when you'd reach for force-refresh — so it
  goes off-pump via the notifier and always fires.
- **`GamePlayHub.ForceRefresh()`** delegates to it; **`game-play-hub.js`** handles
  `ForceRefresh` → `location.reload()`.

---

## 2. 🌟 Cancel Game — POST + broadcast (play **and** setup)

Cancel is **not** a hub method. Decided (John's call) the play-page cancel is a
**plain POST that broadcasts to the hub for the redirect**, so it works in the
HTTP context where `IUserInfo`/`TryCancelGame`'s creator-scoping are reliable
(hub invocations don't run the user-info middleware — every hub method passes
`Context.UserIdentifier` explicitly for this reason).

- **`GameService.TryCancelGame` teardown** — implemented the `//TODO: Clear cache
  and pump`: once the cancellation is committed, an in-play game evicts the cache
  + fire-and-forget stops the pump (mirrors `GameCompletionService.ClearGameRuntime`;
  fire-and-forget because the pump may be the wedged thing). Setup games skip it
  (no live runtime).
- **Play page** — `PlayModel.OnPostCancelAsync` → `TryCancelGame` → on success
  `IEngineNotifier.GameCancelled(gameId)` → `Redirect("/Index")`. The host follows
  the POST redirect; other clients get the **`GameCancelled`** broadcast on
  `GamePlayHub` (`game-play-hub.js` → `/Index`). `_HostPanel.cshtml`'s cancel modal
  confirm is now a `<form method="post" asp-page-handler="Cancel">` (ambient
  `gameId`); removed the dead `CancelGame` hub stub + its host-panel.js wiring.
- **Setup page** — **setup lobbies have live clients too** (host setup page +
  players' lobbies, both on `GameSetupHub`). `SetupModel.OnPostCancelAsync` →
  **`GameSetupService.TryCancelGame`** (already existed — delegates to
  `GameService.TryCancelGame`), which now also broadcasts `GameCancelled` on
  `GameSetupHub`. `game-setup-hub.js` → `/Index`; `game-lobby-hub.js` mirrors the
  `Kicked` flow (lock + "cancelled by host" alert + redirect).
- **`IEngineNotifier.GameCancelled(gameId)`** added (+ impl + the 3 fakes again).

> Churn note: `GameService` was rewritten by John mid-session (added `_logger`,
> `TryDeleteGame`), which twice overwrote my ctor/`ForceRefresh` edits and broke the
> hub build — re-applied cleanly. It now injects `GameCacheService` + `IEngineNotifier`
> alongside the executor/factory/userInfo/logger.

---

## 3. Delete Game — wired the existing stub

`MyGamesModel.OnPostDeleteAsync` now calls **`GameService.TryDeleteGame`** (John
had already written it — soft-deletes game + players + turns + snapshots +
turn-events in one transaction, scoped to the **creator's cancelled** games). The
Delete button already existed in `_GameList` (cancelled + host-only); only the
handler was a TODO. Non-owner / non-cancelled posts no-op, so no extra guard.

---

## 4. Recurring stats job — 03:00 & 15:00 UK

`StatisticsJob` now also runs on a recurring Hangfire schedule
(`AddHangfireJob<StatisticsJob>`, cron `0 3,15 * * *`, `Europe/London` — "every
12h anchored at 3am"), **alongside** the existing fire-and-forget enqueue on
conclusion. Same job class on both ad-hoc + recurring paths (docs bless it;
`AddHangfireScheduler` uses `TryAdd`). It sweeps all finished games and is
idempotent, so the recurring run doubles as the **missing-stats safety-net**.

---

## 5. 🌟 Shared stat catalogue — one source of truth for stat display

The clean-architecture piece behind the comparison page. New, web-layer (engine
record stays UI-agnostic):
- **`Models/Statistics/StatCatalogue.cs`** — `Sections` → `StatSection` →
  `StatDescriptor`(`Label`, `Icon`, `Tone`, `Primary` `StatPart`, optional `Sub`
  free-text, optional `Details` inline badges, optional `Secondary` split). A
  `StatPart` carries `Kind` + `Sentiment` + an `object?` value selector over
  `PlayerStatRecord`. Captures all ~50 stats incl. the two splits (dice number,
  loans), the sub-notes (×passed GO, turn reached), and the largest-payment
  reason/property as inline `Details`.
- **`Models/Statistics/StatRender.cs`** — `Value(part, record, board)` (money /
  number / space pill / set badge / reason badge / yes-no), `AsNumber` (for
  ranking + delta), `Leader` (the standout per row), `Delta` (single-view). The
  value rendering — the bit most likely to drift — is shared; layout (tile vs
  cell) stays per-view.

---

## 6. 🌟 All-players comparison page (`/Games/Compare/{gameId}`)

Reached from a new "Compare All Players" button on the Finished results page.
`CompareModel` loads `GetFinishedGame` (scope/outcomes/seat order) +
`GetGameStatistics(gameId)` (already existed) + `PlayerCacheService.GetPlayerProfile`.
- **Table** — one column per player in **seat order**; header = `profile-circle` +
  icon-only **outcome badge** + name on `title` hover. **Sticky header row + sticky
  first column**, **collapsible sections (expanded by default)**, **table-striped**,
  with a **subtle translucent-info leader tint** per numeric row.
- Sub-notes/`Details` render **inline in the cell**; **split** stats become **two
  rows each**.
- **Blocked players**: stats stay, identity masked with an **"Unknown"** placeholder
  VM (`new UserProfileViewModel("", …)`, empty id ⇒ no link, doesn't reveal
  direction; `CheckIfBlocksExist` is bidirectional).
- **Charts** — four per-player overlaid line graphs (balance / net worth /
  properties / wealth-rank, rank inverted), with a **fixed 6-colour palette** (per
  player, consistent across charts): orange `#fd7e14`, green `#40c057`, cyan
  `#0dcaf0`, red `#fa5252`, purple `#cc5de8`, gold `#ffc107`.
- Styles added to `player-stats.css` (`.stat-compare-*`).

---

## 7. Catalogue tweaks (post-review by John)

- **Neutral stats now highlight the highest.** `StatRender.Leader` ranks
  `LowerBetter` on the lowest and **everything else (incl. Neutral) on the highest**,
  and returns null when everyone's tied (so all-equal/all-zero rows don't light up).
  Done in `Leader`, not by flipping sentiments — keeps those stats' deltas grey when
  the single view later moves onto the catalogue.
- **Voluntary bankruptcy → tri-state.** New `StatKind.TriBool`: **Yes = green, No =
  red, N/A (grey)** when the player never went bankrupt. Catalogue value resolves to
  `null` (→ N/A) unless `Bankrupted`, then the voluntary flag, coloured `HigherBetter`.

---

## 8. Finished page — same masking + hide dead actions

- `FinishedModel` now masks blocked players the same way (inject
  `BlockAndReportService`; blocked → "Unknown" VM; outcome/placement kept).
- **`_PlayerCard`**: the non-setup action (View Stats / social View) now only renders
  when `Profile.UserId` is present — so masked cards show **no button** (the empty id
  on the placeholder VM is the signal; **no record/flag change** needed).

---

## 9. Play-page & board polish

- **Player list scrolls like the other panes.** `.play-player-card` got
  `flex-shrink: 0` — it's a flex column with `overflow-auto`, so without it the cards
  squashed to fit the pane instead of the pane scrolling.
- **Board rent-level indicator.** On owned (`PropertyState.Owned`) rectangles, a
  centred overlay **beneath the player chips** (`z-index 1` vs chips' `2`): building
  **squares** for built levels (1–4 green houses, hotel/double-hotel red, **max 2 per
  row**), else a rounded **secondary badge** — `SINGLE`="1", `SET`="S", `DOUBLE`="D",
  `TRIPLE`="T" (also covers stations/utilities). New `RentLevelVisual` helper +
  `.space-buildings` / `.building` / `.rent-badge` CSS in `_BoardView.cshtml`.

---

## 10. TODO / unfinished-work review

Swept the codebase. Findings:
- **Stale comments to fix** (now wrong after this session): `host-panel.js:8`,
  `_HostPanel.cshtml:10` (Force Refresh/Cancel "still TODO" — they're done),
  `Play.cshtml.cs:15` ("once SignalR wired — see the page's TODO"). **Offered, not
  yet fixed.**
- **Deferred subsystems (expected):** cards (the whole mini-engine — engine
  subsystem TODOs, card stat services reading 0, card prompts/receipts/import);
  purge service (`FreeParkingService` case C, `PropertiesPurged` always 0).
- **Mock/stub UI:** `_PlayerProfileView` jail "use card" + %cap tier helper + Offer
  Deal stub; `player-page.js` `[data-noop]`; **Chat is entirely dummy data**
  (`Chat.cshtml.cs`); navbar **Messages / Leaderboard** are `href="#"`.
- **`Pages/Index.cshtml` is a Bootstrap kitchen-sink demo** — worth confirming/replacing
  as the landing page.
- Minor open decisions: `BlockAndReportService` unblock→friend re-add (×2),
  `ProfileService` "extend with game pieces".

---

## 11. What's left / known debts

- **#6 is half-done:** the catalogue + `StatRender` exist and the **comparison page
  uses them**, but the single-player **`_PlayerStatsPartial` is NOT yet refactored
  onto the catalogue** (still hard-coded). Deliberately deferred — it's the riskiest
  edit (a "works perfectly" page under a hard "don't change the look" constraint;
  can't visually diff). Plan agreed: a shared `_StatTile.cshtml` (+ `StatTileModel`)
  reproducing the exact tile markup, then catalogue loops replacing the ~50 inline
  calls, **keeping every card shell / column class / hero / charts identical**.
- The **stale comments** from §10 to correct.
- **Cards** and **purging** — still the monsters (unchanged).

---

## 12. Memories saved
- `project_stats_comparison` — the shared `StatCatalogue` + `StatRender` and the
  `/Games/Compare/{gameId}` page are built and consume the catalogue; the
  single-player `_PlayerStatsPartial` is **not yet** on the catalogue (deferred,
  must stay pixel-identical when done). Links the host-control wiring pattern.
- `project_host_controls` — host game controls live in `GameService`; Force Refresh
  + Cancel broadcast via `IEngineNotifier` (`ForceRefresh` / `GameCancelled`)
  **off the pump**, Cancel is a POST (play → `GamePlayHub`, setup →
  `GameSetupHub`), Delete = `TryDeleteGame`. Touching `IEngineNotifier` means
  updating the 3 `NoOpNotifier` test fakes.