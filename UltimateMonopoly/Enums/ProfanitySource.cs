namespace UltimateMonopoly.Enums;

/// <summary>Which layer flagged the input — for the friendly error surface and the audit trail.</summary>
public enum ProfanitySource
{
    None,
    Library,
    LocalList
}